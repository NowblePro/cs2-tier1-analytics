# Usually leave empty: Vite proxies /api to FastAPI during development and
# production can use the same origin.
VITE_API_BASE_URL=

# true = skip network calls and use bundled representative data.
VITE_DEMO_MODE=false
